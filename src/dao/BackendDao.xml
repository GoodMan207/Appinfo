<?xml version="1.0" encoding="utf-8"?>
 <!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.BackendDao">
	<select id="DoLogin" parameterType="String" resultType="User">
	select i.*,d.valueName as userTypeName from backend_user i INNER JOIN data_dictionary d ON d.id=i.userType 
		<trim prefix="where" prefixOverrides="and | or">
			<if test ="userCode!=null">
				and userCode=#{userCode}
			</if>
		</trim>
	</select>
	
	<select id="BackShowAppList" resultType="Info">
		select a.id, a.softwareName,a.APKName,a.supportROM,a.softwareSize,a.devId,
				(select devName from dev_user where id = a.devId) as devName,
				a.status,
				(select valueName from data_dictionary d where  a.status=d.valueId AND d.typeCode='APP_STATUS') as statusName,
				a.flatformId,
				(select valueName from data_dictionary d where  a.flatformId=d.valueId AND d.typeCode='APP_FLATFORM') as flatformName,
				a.categoryLevel1,
				(select categoryName from app_category c where  c.id=a.categoryLevel1) as categoryLevel1Name,
				a.categoryLevel2,
				(select categoryName from app_category c where  c.id=a.categoryLevel2) as categoryLevel2Name,
				a.categoryLevel3,
				(select categoryName from app_category c where  c.id=a.categoryLevel3) as categoryLevel3Name,
				a.downloads,
				a.onSaleDate,
				a.versionId,
				(select v.versionNo from app_version v where v.id=a.versionId ) as versionNo
			from  app_info a
	</select>
	
	<select id="flatForms" parameterType="String" resultType="Dictionary">
		SELECT d.* FROM data_dictionary d WHERE typeCode='APP_FLATFORM'
	</select>
</mapper>